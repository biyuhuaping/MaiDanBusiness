//
//  OedTableViewCell.m
//  DaJiaZhuanSH
//
//  Created by feng on 15/10/15.
//  Copyright © 2015年 feng. All rights reserved.
//

#import "OedTableViewCell.h"

@implementation OedTableViewCell

- (void)awakeFromNib {
    // Initialization code
    _totalfee.layer.masksToBounds=YES;
    _totalfee.layer.cornerRadius=10;
    [_totalfee sizeToFit];
    _totalfee.layer.borderColor=[RGBCOLOR(241, 89, 42)CGColor];
    _totalfee.layer.borderWidth=1;
    
    _feerate.layer.masksToBounds=YES;
    _feerate.layer.cornerRadius=10;
    [_feerate sizeToFit];
    _feerate.layer.borderColor=[RGBCOLOR(241, 89, 42)CGColor];
    _feerate.layer.borderWidth=1;
    
    _feerate.layer.masksToBounds=YES;
    _payType.layer.cornerRadius=10;
    [_feerate sizeToFit];
    _payType.layer.borderColor=[RGBCOLOR(241, 89, 42)CGColor];
    _payType.layer.borderWidth=1;
    
    _bgView.layer.masksToBounds=YES;
    _bgView.layer.cornerRadius=10;
    _bgView.layer.borderColor=[RGBCOLOR(204, 204, 204)CGColor];
    _bgView.layer.borderWidth=1;
    
    self.backgroundColor=RGBCOLOR(245, 245, 245);
}

- (void)setSelected:(BOOL)selected animated:(BOOL)animated {
    [super setSelected:selected animated:animated];

    // Configure the view for the selected state
}

@end
